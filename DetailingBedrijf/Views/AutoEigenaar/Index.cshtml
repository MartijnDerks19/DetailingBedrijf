@model IEnumerable<LogicaLaag.Models.AutoEigenaarModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Aanmaken">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EigenaarID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EigenaarID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Naam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.ActionLink("Auto's ophalen", "AutosVanEigenaarOpID", "Auto",new { id = item.EigenaarID }) |
                @Html.ActionLink("Details", "Details", new { id = item.EigenaarID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.EigenaarID })
            </td>
        </tr>
}
    </tbody>
</table>
